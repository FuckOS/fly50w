<?php

$(macro) {
    tryit ($(layer() as try_expr)) {
        $(layer() as body_try)
    } except ( $(layer() as except_expr) ) {
        $(layer() as body_except)
    }
} >> {
    if ($(try_expr)) {
        $(body_try)
    } elseif ($(except_expr)) {
        $(body_except)
    }
}